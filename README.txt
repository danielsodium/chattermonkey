To change the username for twitch and tiktok, change the json values in "username.json".
When running for the first time, run init.bat and close the window when you see a blinking cursor.
To run normally run start.bat.
You can change the colors by going into twitch.css and scrolling to the bottom.